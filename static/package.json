{
    "name": "dusk",
    "version": "0.0.1",
    "description": "dusk static assets",
    "main": "src/index.ts",
    "repository": "https://github.com/lacunaverse/dusk",
    "author": "hvlck",
    "license": "GPL-3.0",
    "private": true,
    "devDependencies": {
        "esbuild": "^0.15.5"
    },
    "scripts": {
        "build": "esbuild --bundle ./src/*.ts --outdir=dist --minify",
        "watch": "yarn run build --watch"
    }
}